CREATE DEFINER=`root`@`localhost` PROCEDURE `IngresarSede`(
	IN `P_IdSede` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
INSERT INTO Departamento(IdSede) 
	VALUES (P_IdSede );
END