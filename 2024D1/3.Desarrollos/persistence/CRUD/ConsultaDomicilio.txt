CREATE DEFINER=`root`@`localhost` PROCEDURE `ConsultaDomicilio`()
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
SELECT IdDomicilio, Activo, Actualiza
	FROM Domicilio
	WHERE Activo = 1;
END