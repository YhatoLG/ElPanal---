CREATE DEFINER=`root`@`localhost` PROCEDURE `ConsultaSede`()
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
SELECT IdSede, Activo, Actualiza
	FROM Sede
	WHERE Activo = 1;
END