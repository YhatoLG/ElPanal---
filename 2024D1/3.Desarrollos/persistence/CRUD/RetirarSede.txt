CREATE DEFINER=`root`@`localhost` PROCEDURE `RetirarSede`(
	IN `P_IdSede` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
UPDATE Sede
	SET Activo = 0,
   	 Actualiza = CURRENT_TIMESTAMP()
	WHERE IdSede = P_IdSede
   	 AND Activo = 1;
END