CREATE DEFINER=`root`@`localhost` PROCEDURE `ConsultaFactura`()
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
SELECT IdFactura, Activo, Actualiza
	FROM Factura
	WHERE Activo = 1;
END