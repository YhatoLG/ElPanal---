CREATE DEFINER=`root`@`localhost` PROCEDURE `RetirarUsuarios`(
	IN `P_IdUsuario` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
UPDATE Usuarios
	SET Activo = 0,
   	 Actualiza = CURRENT_TIMESTAMP()
	WHERE IdUsuario = P_IdUsuario
   	 AND Activo = 1;
END