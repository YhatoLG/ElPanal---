CREATE DEFINER=`root`@`localhost` PROCEDURE `RetirarCiudad`(
	IN `P_IdCiudad` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
UPDATE Ciudad
	SET Activo = 0,
   	 Actualiza = CURRENT_TIMESTAMP()
	WHERE IdCiudad = P_IdCiudad
   	 AND Activo = 1;
END