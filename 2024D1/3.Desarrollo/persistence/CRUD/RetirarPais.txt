CREATE DEFINER=`root`@`localhost` PROCEDURE `RetirarPais`(
	IN `P_IdPais` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
UPDATE Pais
	SET Activo = 0,
   	 Actualiza = CURRENT_TIMESTAMP()
	WHERE IdPais = P_IdPais
   	 AND Activo = 1;
END