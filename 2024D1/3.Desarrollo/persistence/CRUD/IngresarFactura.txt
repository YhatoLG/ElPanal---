CREATE DEFINER=`root`@`localhost` PROCEDURE `IngresarFactura`(
	IN `P_IdFactura` CHAR(36)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN
INSERT INTO Factura(IdFactura) 
	VALUES (P_IdFactura);
END